const getClients = require('../client/clientCase/getClients/getClients');
const createClient = require('../client/clientCase/createClient/createClient');
const signIn = require('../client/clientCase/signIn/signIn');

module.exports = {
    getClients,
    createClient,
    signIn
}